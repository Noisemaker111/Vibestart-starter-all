---
description: 
globs: 
alwaysApply: true
---
Use Cursor to maintain the live directory map in this file, ensuring its always accurate and up-to-date. Immediately update this after any filesystem change. Keep each entry to a single, concise sentence describing its purpose, with no extraneous comments.


# Project Structure — jonstack
src/ — all source code
src/client/ — React 19 browser app (SSR)
src/client/components/ — reusable UI primitives
src/client/components/Header.tsx — nav bar & theme toggle
src/client/components/CreateOrganizationButton.tsx — org modal (auth-gated)
src/client/components/SignInButton.tsx — login / logout trigger
src/client/components/SignInForm.tsx — email+pwd & Google OAuth form
src/client/components/ThemeToggle.tsx — light/dark switch
src/client/components/SquareUploadButton.tsx — UploadThing button
src/client/components/IdeaCarousel.tsx — rotating suggestions
src/client/context/AuthContext.tsx — Supabase session provider
src/client/pages/home.tsx — marketing landing page
src/client/pages/docs.tsx — in-app docs hub
src/client/pages/test.tsx — integration playground (DB/Auth/Uploads)
src/client/pages/api.animals.tsx — edge bridge → server/animals
src/client/pages/api.images.tsx — edge bridge → list uploads
src/client/pages/api.uploadthing.tsx — edge bridge → uploadthing router
src/client/utils/rateLimit.ts — syncs rate-limit headers to UI
src/client/utils/uploadthing.ts — UploadThing React helpers
src/client/root.tsx — Remix-style root layout
src/client/routes.ts — static route map
src/server/ — server / edge runtime code
src/server/db/ — Drizzle ORM & migrations
src/server/db/schema.ts — single source-of-truth tables
src/server/db/index.ts — Drizzle client init (.env loaded)
src/server/animals.ts — REST-like handler for animals
src/server/uploadthing.ts — UploadThing upload router
src/server/utils/anonToken.ts — HMAC-signed anon cookies
src/server/utils/rateLimit.ts — unified DB-backed rate limiter
src/shared/ — isomorphic helpers
src/shared/supabase.ts — Supabase client factory
src/shared/appIdeas.ts — 100+ placeholder idea strings
src/shared/app.css — Tailwind entrypoint & utilities
public/ — static assets
vite.config.ts — Vite SSR config & aliases
tailwind.config.js — Tailwind v4 config
tsconfig.json — TS paths & compiler options
drizzle.config.ts — Drizzle migration config
package.json — dependencies & npm scripts
README.md — project overview

