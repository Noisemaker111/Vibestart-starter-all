# photo, pdf, mp4 upload site

UPLOADTHING_TOKEN='ut_1234567890'

# supabase database

VITE_SUPABASE_URL='https://your-supabase-url.supabase.co'
VITE_SUPABASE_ANON_KEY='your-supabase-anon-key'
DATABASE_URL='postgresql://user:password@localhost:5432/dbname'

# openrouter api key for text and json llm generation

VITE_OPENROUTER_API_KEY='or_1234567890'

# posthog analytics

VITE_PUBLIC_POSTHOG_KEY='phc_1234567890'
VITE_PUBLIC_POSTHOG_HOST='https://us.i.posthog.com'

# openai api key for image generation

OPENAI_API_KEY='sk-1234567890'

# site url (used for AI request referer)
VITE_SITE_URL='http://localhost:5173'

# supabase oauth redirect url
VITE_SUPABASE_REDIRECT='http://localhost:5173'
# supabase oauth redirect dev url

VITE_SUPABASE_REDIRECT_DEV_URL='http://localhost:5173'

# openrouter api key (server-side)
OPENROUTER_API_KEY='or_1234567890'


# polar billing (payments)
POLAR_ACCESS_TOKEN='polar_xxx'
POLAR_WEBHOOK_SECRET='whsec_xxx'

# resend email
RESEND_API_KEY='resend_1234567890'

# Hash of the authorised developer email (FNV-1a 32-bit).
# Generate in a terminal with one of the following:
# 1. Quick inline Node script:
#    node -e "const f=s=>{let h=0x811c9dc5;for(const c of s){h^=c.charCodeAt(0);h=(h*0x1000193)>>>0;}console.log(h.toString(16));};f('dev@example.com');"
# 2. Inside a browser console:
#    (()=>{let h=0x811c9dc5;for(const c of 'dev@example.com'){h^=c.charCodeAt(0);h=(h*0x1000193)>>>0;}console.log(h.toString(16));})();
# Replace 'dev@example.com' with your own address and copy the resulting hex string below.
VITE_DEV_EMAIL_HASH='replace_with_fnv_hash'
GOOGLE_MAPS_API_KEY='google_maps_api_key'